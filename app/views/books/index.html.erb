<div class="row">

<%= render 'users/template', user: @user, book: @book %>

		<div class="col-sm-9">
			<h2>Books</h2>
			<table class="table">
				<thead>
					<tr>
						<th></th>
						<th>Title</th>
						<th>opinion</th>
					</tr>
				</thead>
				<tbody>
				<% @books.each do |book| %>
					<tr>
						<td>
						</td>
						<td>
						<%= link_to book.title, book_path(book.id) %>
						</td>
						<td>
						<%= book.body %>
						</td>
					</tr>
				<% end %>
				</tbody>
			</table>
		</div>

		<%= render 'users/template2' %>