<div class="row">

<%= render 'users/template', user: @user, book: @book %>


<div class="col-xs-9">
	<h2>Books</h2>
	<table class="table table-hover table-inverse">

		<tr>
			<th></th>
			<th>Title</th>
			<th>opinion</th>
		</tr>

		<% @books.each do |book| %>
		
		<tr>
			<td><%= user.name %></td>

			<td><%= book.title %></td>
			
			<td><%= book.body %></td>
		</tr>
		
		<% end %>
	</table>
</div>
</div>

		